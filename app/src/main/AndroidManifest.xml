<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.johnny.a">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:icon="@mipmap/tt2"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".Hodlogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sign_in"></activity>
        <activity android:name=".Intro"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".TextViewController"></activity>
    </application>

</manifest>